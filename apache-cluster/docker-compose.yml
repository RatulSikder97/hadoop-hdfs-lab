# Cluster using the OFFICIAL Apache Hadoop images (3.4.1), as an alternative to
# the bde2020 cluster in the parent folder. Each role is selected via `command`,
# config comes from ./config, and a real NodeManager is included so YARN runs
# actual containers (not just local mode).
#
# Every service builds from ./Dockerfile, which adds python3 to the base image
# (only if missing) so the Hadoop Streaming job can run. The image is built once
# and shared across all services via the `image:` tag.
#
#   Run from this folder:
#     Start:  docker compose up -d        (first run builds the image)
#     Stop:   docker compose down
#
# The Hadoop Streaming jar here is at
# /opt/hadoop/share/hadoop/tools/lib/hadoop-streaming-3.4.1.jar
services:
  namenode:
    build:
      context: .
      dockerfile: Dockerfile
    image: hadoop-apache-python:local
    hostname: namenode
    container_name: namenode
    command: ["hdfs", "namenode"]
    ports:
      - "9870:9870"   # NameNode web UI
      - "8020:8020"   # HDFS RPC
    env_file:
      - ./config
    environment:
      ENSURE_NAMENODE_DIR: "/tmp/hadoop-root/dfs/name"

  datanode:
    image: hadoop-apache-python:local
    container_name: datanode
    command: ["hdfs", "datanode"]
    depends_on:
      - namenode
    env_file:
      - ./config

  resourcemanager:
    image: hadoop-apache-python:local
    hostname: resourcemanager
    container_name: resourcemanager
    command: ["yarn", "resourcemanager"]
    ports:
      - "8088:8088"   # YARN web UI
    depends_on:
      - namenode
      - datanode
    env_file:
      - ./config

  nodemanager:
    image: hadoop-apache-python:local
    container_name: nodemanager
    command: ["yarn", "nodemanager"]
    depends_on:
      - resourcemanager
    env_file:
      - ./config
